# JVS-APS 高级计划与排程系统 — 技术复现报告

## 一、系统概述

### 1.1 定位
APS（Advanced Planning and Scheduling）是连接 ERP 和 MES 的核心中间层，负责将生产订单转化为可执行的、时间和资源最优的生产计划。系统通过约束优化求解器，在满足资源容量、工艺路线、物料齐套等硬约束的前提下，对交期、效率、负载均衡等软目标进行多目标优化。

### 1.2 核心价值
- 自动化：消除手工排产的低效和不一致
- 优化求解：使用专业约束求解器（OptaPlanner），获得全局近优解
- 动态响应：支持插单、锁定任务、任务拆分/合并/移动等排后调整
- 可视化：甘特图（资源维度、订单维度）+ 物料需求图 + 任务列表

### 1.3 技术栈摘要

| 层级 | 技术选型 |
|------|----------|
| 后端框架 | Spring Boot 2.7.x |
| ORM | MyBatis-Plus 3.5.x |
| 求解引擎 | OptaPlanner 9.44 |
| 数据库 | MySQL 8.0 |
| 缓存 | Redis 6.0 |
| 前端框架 | Vue 3 + TypeScript + Vite 5 |
| UI组件 | Element Plus 2.8 |
| 甘特图/表格 | VTable 1.15 |
| 图形可视化 | AntV G6 5.0 |
| 运行环境 | Java 17, Node.js |
| 容器化 | Docker + Docker Compose |

---

## 二、系统架构

### 2.1 模块划分

```
jvs-aps/
├── aps/
│   ├── jvs-aps-common/    # 核心业务库（领域模型、服务、求解器、图算法）
│   ├── jvs-aps-mgr/       # Web应用层（REST控制器、应用配置）
│   └── sql/               # 数据库初始化脚本
├── aps-ui/                # 前端SPA应用
└── jvs-docker-compose/    # 容器编排
```

### 2.2 后端分层架构

```
Controller（REST API）
    ↓
Facade Service（聚合服务，编排复杂业务流程）
    ↓
Domain Service（领域服务，单实体 CRUD）
    ↓
Mapper（数据访问层，MyBatis-Plus）
    ↓
MySQL（持久化）+ Redis（缓存/锁/进度跟踪）
```

### 2.3 核心子系统

1. **基础数据管理**：物料、BOM、资源、工序、工艺路线、日历
2. **生产订单管理**：订单生命周期、优先级、排产参与控制
3. **排产策略管理**：方向配置、排序规则、优化权重
4. **智能排产引擎**：MRP物料需求计划 + OptaPlanner约束优化
5. **排产结果管理**：待确认/已确认双表设计、甘特图报告
6. **排后调整**：任务冻结、合并、拆分、移动

---

## 三、领域模型（数据库 ER 设计）

### 3.1 核心实体关系

```
ProductionOrder ──────────→ Material（通过materialCode关联）
       │                         │
       │                         ↓
       │                   ManufactureBom（material_id → 子件列表JSON）
       │                         │
       │                         ↓
       │                   ProcessRoute（material_id → 工艺路线图JSON）
       │                         │
       │                         ↓
       │                     Process（工序定义，含可用资源列表）
       │                         │
       ↓                         ↓
PlanTaskOrder ←── PlanTask ──→ ProductionResource
                     │
                     ↓
              WorkCalendar（通过ResourceCalendar关联资源）
                     │
                     ↓
                 WorkMode（工作时间段）
```

### 3.2 表结构定义

#### 3.2.1 物料表 `jvs_material`
| 字段 | 含义 | 约束 |
|------|------|------|
| id | 主键 | UUID |
| name | 物料名称 | |
| code | 物料编码 | 租户级唯一 |
| type | 类型 | 枚举：RAW_MATERIAL / SEMI_FINISHED / FINISHED |
| source | 来源 | 枚举：PRODUCED（自制）/ PURCHASED（外购）|
| quantity | 当前库存 | decimal(18,6) |
| safety_stock | 安全库存 | decimal(18,6) |
| unit | 计量单位 | |
| lead_time | 提前期 | 时长字符串，如 "2D"、"8H" |
| buffer_time | 缓冲期 | 时长字符串 |
| tenant_id | 租户ID | 多租户隔离 |

#### 3.2.2 制造BOM表 `jvs_manufacture_bom`
| 字段 | 含义 |
|------|------|
| id | 主键 |
| material_id | 成品物料ID |
| child_materials | 子件列表（JSON数组）[{materialId, quantity}] |

#### 3.2.3 工序表 `jvs_process`
| 字段 | 含义 |
|------|------|
| id | 主键 |
| name/code | 名称/编码 |
| pre_interval_duration | 前间隔时长（如 "60M"） |
| post_interval_duration | 后间隔时长 |
| process_relationship | 工序关系：ES（结束-开始）/ EE（结束-结束）|
| buffer_time | EE关系下的缓冲时长 |
| batch_strategy | 批量策略（JSON） |
| use_main_resources | 可用主资源列表（JSON）[{id, capacity, throughput}] |
| use_auxiliary_resources | 可用辅助资源（JSON） |
| use_materials | 输入物料（JSON）[{id, use, quantity}] |

#### 3.2.4 工艺路线表 `jvs_process_route`
| 字段 | 含义 |
|------|------|
| material_id | 关联物料ID（一对一） |
| route_design | 工艺路线图（JSON，包含nodes和edges） |
| enabled | 是否启用 |

**route_design结构**：有向无环图（DAG），nodes为工序节点（含工序属性），edges表示工序间的先后关系（source→target）。

#### 3.2.5 生产资源表 `jvs_production_resource`
| 字段 | 含义 |
|------|------|
| id/name/code | 标识 |
| resource_group | 资源组 |
| capacity | 容量（并行处理能力）|
| unit | 容量单位 |
| throughput | 默认产能 |
| resource_status | 状态：NORMAL / MAINTENANCE / SCRAPPED |

#### 3.2.6 生产订单表 `jvs_production_order`
| 字段 | 含义 |
|------|------|
| code | 订单编码 |
| material_code | 目标物料编码 |
| quantity | 需求数量 |
| delivery_time | 交付期限 |
| priority | 优先级（整数，越大越优先）|
| sequence | 排序序号 |
| type | 订单类型：MANUFACTURE |
| order_status | 订单状态：PENDING / COMPLETED / CANCELLED |
| scheduling_status | 排产状态：UNSCHEDULED / SCHEDULED / COMPLETED / NO_SCHEDULED |
| can_schedule | 是否参与排产 |
| supplement | 是否为补充订单 |
| parent_order_code | 父订单编码（补充订单专用）|
| color | 甘特图显示颜色 |

#### 3.2.7 排产计划任务表 `jvs_plan_task`
| 字段 | 含义 |
|------|------|
| code | 任务编码（租户级唯一）|
| merge_task_code | 合并后的任务编码 |
| origin_task_code | 拆分来源任务编码 |
| main_order_id | 主生产订单ID |
| production_order_id | 所属生产订单ID |
| scheduled_quantity | 计划生产数量 |
| process_code | 工序编码 |
| process_info | 工序信息快照（JSON）|
| front_task_codes | 前置任务编码集合（JSON数组）|
| next_task_codes | 后置任务编码集合（JSON数组）|
| main_resource_id | 分配的主资源ID |
| start_time / end_time | 计划开始/结束时间 |
| start_task / end_task | 是否首道/末道工序 |
| pinned | 是否锁定（锁定后重排不变）|
| merge_task | 是否为合并任务 |
| input_materials | 输入物料（JSON）|
| task_status | 状态：PENDING / PARTIALLY_COMPLETED / COMPLETED |

#### 3.2.8 待确认表（Pending模式）
系统采用**双表设计**：
- `jvs_plan_task_pending`：待确认任务（结构同 plan_task）
- `jvs_plan_task_order_pending`：待确认订单信息

排产完成后先写入pending表，用户确认后再原子转储至正式表。

#### 3.2.9 任务调整表 `jvs_plan_task_adjust`
存储排后手动调整的任务（冻结/合并/拆分/移动后的结果），结构基本同plan_task，增加：
- `discard`：是否丢弃
- `compliant`：是否合规

#### 3.2.10 工作日历 `jvs_work_calendar` + `jvs_work_mode`
| 字段（日历）| 含义 |
|------|------|
| name | 日历名称 |
| work_mode_id | 关联工作模式 |
| begin_time / end_time | 有效期间 |
| enabled | 是否启用 |
| work_days | 位图7位（周日→周六，每位1=工作日）|
| priority | 优先级（高优先级覆盖低优先级）|

**工作模式**：定义一天中的工作时间段（如 08:00-12:00, 13:00-17:00）。

#### 3.2.11 排产策略表 `jvs_planning_strategy`
| 字段 | 含义 |
|------|------|
| name | 策略名称 |
| begin_time | 排产开始时间 |
| active | 是否有效 |
| config | 策略配置（JSON）：direction(FORWARD/BACKWARD), materialConstrained, maxNoImprovementTime |
| order_scheduling_rules | 订单排序规则（JSON数组）：[{fieldKey, sortRule(ASC/DESC)}] |
| optimize_rules | 优化规则权重（JSON数组）：[{constraintKey, weight}] |

---

## 四、核心算法 — 智能排产引擎

### 4.1 排产流程总览

```
用户触发排产
    │
    ▼
┌─────────────────────────────┐
│  1. 策略加载与数据聚合        │
│  - 加载策略配置              │
│  - 收集物料、BOM、资源、       │
│    工艺路线、日历、订单        │
│  - 收集已锁定任务            │
└─────────────────────────────┘
    │
    ▼
┌─────────────────────────────┐
│  2. MRP 物料需求计划          │
│  - BOM展开                   │
│  - 库存齐套检查              │
│  - 缺料补充订单生成           │
└─────────────────────────────┘
    │
    ▼
┌─────────────────────────────┐
│  3. 任务生成                  │
│  - 工艺路线拓扑排序生成任务    │
│  - 建立任务前后置依赖关系      │
│  - 已锁定任务匹配与保留       │
└─────────────────────────────┘
    │
    ▼
┌─────────────────────────────┐
│  4. 约束优化求解              │
│  - 构造Solution模型          │
│  - 配置硬约束和软约束         │
│  - OptaPlanner异步求解       │
│  - 进度实时追踪              │
└─────────────────────────────┘
    │
    ▼
┌─────────────────────────────┐
│  5. 结果校验与持久化          │
│  - 校验所有任务已分配资源      │
│  - 校验时间合规              │
│  - 保存至待确认表             │
└─────────────────────────────┘
    │
    ▼
┌─────────────────────────────┐
│  6. 用户确认                  │
│  - 预览甘特图/任务列表        │
│  - 确认后转储至正式表         │
└─────────────────────────────┘
```

### 4.2 MRP 物料需求计划

#### 算法逻辑

```
输入：生产订单列表、BOM映射、物料库存、来料订单
输出：缺料物料有向无环图（用于生成补充生产任务）

对每个生产订单：
  1. 从订单目标物料开始，按BOM层级递归展开
  2. 每一层，计算净需求 = 毛需求 - 可用库存 - 在途数量
  3. 库存扣减顺序：
     a. 先扣在库库存（当前库存量）
     b. 再扣在途库存（来料订单，按到货时间排序）
  4. 若净需求 > 0 且物料为自制件（source=PRODUCED）：
     - 创建缺料节点
     - 建立与上层物料的依赖边
  5. 递归处理下层子件
  6. 最终返回缺料DAG图
```

#### 库存扣减细节
- 来料订单按预计到货时间升序排列
- 扣减时考虑物料的提前期（lead_time）和缓冲期（buffer_time）
- 部分满足的订单只扣减可用数量

### 4.3 任务生成

#### 从缺料图到任务

```
对缺料DAG进行拓扑排序（Kahn算法），按序处理每个缺料物料：
  1. 查找该物料的工艺路线（DAG图结构）
  2. 若物料非订单主产物，创建补充生产订单
  3. 对工艺路线图进行拓扑排序
  4. 为每个工序节点创建一个ProductionTask：
     - 关联生产订单
     - 设置生产数量
     - 设置工序属性（从route_design节点的data中获取）
  5. 根据工艺路线边关系建立任务间前后置依赖
  6. 根据BOM层级建立跨物料任务间的前后置依赖（上层末道工序 ← 下层首道工序）
```

#### 已锁定任务匹配
重排时，系统保留已锁定（pinned=true）的任务：
1. 根据任务编码精确匹配新旧任务
2. 若锁定任务的剩余数量 < 新任务计划数量：保留锁定任务并对差量部分生成新任务
3. 若锁定任务的剩余数量 >= 新任务计划数量：仅保留锁定任务
4. 修正前后置关系，将锁定任务作为新任务的前置

### 4.4 约束优化模型

#### 规划模型定义

| 概念 | 映射 | 说明 |
|------|------|------|
| Solution | SchedulingSolution | 包含所有资源和任务的完整方案 |
| Planning Entity | MainProductionResource | 每个资源维护一个有序任务列表 |
| Planning Variable | taskList（List Variable） | 资源上分配的任务列表（有序）|
| Planning Pin | ProductionTask.pinned | 锁定的任务不被优化器移动 |
| Shadow Variable | startTime / endTime | 由监听器根据分配结果计算 |
| Score | HardSoftLongScore | 硬分+软分复合评分 |

#### 硬约束（违反则方案不可行）

| 约束名 | 规则 |
|--------|------|
| equipmentCanOnlyHandleMatchingProcesses | 任务的工序必须在分配资源的可用工序列表中 |
| noResourceTimeOverlapping | 同一资源上的任务时间段不能重叠 |
| theEquipmentCanOnlyProcessOneProcessAtTheSameTime | 每个任务必须被分配资源并有明确的开始/结束时间 |

#### 软约束（有权重的优化目标）

| 约束Key | 含义 | 典型权重 |
|---------|------|----------|
| scheduling_rules | 按策略排序规则排列（优先级、交期等）| 54 |
| minimize_tardiness | 最小化订单延期总时长 | 20 |
| minimize_tardiness_task_number | 最小化延期任务数量 | 1 |
| prioritize_delivery_time | 优先安排临近交期的订单 | 1 |
| prioritize_short_task | 优先安排短任务（SPT规则）| 0 |
| minimize_slack_time | 最小化松弛时间 | 0 |
| priority_critical_ratio | 临界比规则 | 0 |
| minimize_gap_between_dependent_tasks | 最小化有依赖关系任务间的间隔 | 1 |
| finish_early | 尽早完成所有任务 | 6 |
| balance_task_count | 均衡各资源的任务数量 | 15 |
| balance_workload | 均衡各资源工作负载 | 0 |

权重通过策略表的`optimize_rules`字段配置，支持用户自定义调整。

### 4.5 时间计算（Shadow Variable）

#### 开始时间计算器（ApsStartTimeListener）

当OptaPlanner改变任务的资源分配或顺序时，自动触发开始时间重新计算：

```
对资源R上位置index处的任务T：
  1. 确定最早可开始时间 = max(
       策略开始时间,
       所有前置任务的结束时间 + 后间隔时长,
       物料到料时间（若有物料约束）,
       前一任务结束时间（同资源连续约束）
     )
  2. 根据工作日历找到最早可开工时间点：
     - 跳过非工作日
     - 跳过工作时间段外的时间
  3. 计算任务持续时长 duration = f(产能格式, 数量)
  4. 根据工作日历和duration计算结束时间：
     - 若duration跨越休息时间，需累加休息间隔
     - 若跨越非工作日，需跳过
  5. 应用前间隔/后间隔时长
```

#### 产能格式与时长计算

三种产能表达格式：

| 格式 | 示例 | 计算公式 |
|------|------|----------|
| 处理单个需要多久 | "5M"（单个5分钟）| duration = throughput × quantity |
| 单位时间处理多少 | "10/H"（每小时10个）| duration = quantity ÷ rate × timeUnit |
| 一批需要多久 | "30M/P"（一批30分钟）| duration = throughput（不考虑数量）|

时长单位代码：D=天, H=小时, M=分钟, S=秒

#### 工序关系对时间的影响

| 关系类型 | 含义 | 时间约束 |
|---------|------|----------|
| ES（End-to-Start）| 前工序结束后才能开始后工序 | next.start >= prev.end + postInterval |
| EE（End-to-End）| 允许并行，后工序可以和前工序重叠 | next.start >= prev.start + bufferTime |

### 4.6 求解器配置

```
求解器参数：
- 约束流实现：BAVET（高性能增量计算）
- 终止条件：
  - 最大求解时间 1小时（硬上限）
  - 最大无改进时间（策略可配，如5分钟）
- 多线程：AUTO（自动检测CPU核心数）
- 每租户独立 SolverManager 实例
```

### 4.7 进度追踪机制

```
使用Redis实现排产进度的实时追踪：
- Key结构：tenant:{tenantId}:solve:progress
- 数据结构：Hash（状态、进度百分比、步骤数、开始时间）
- 日志列表：tenant:{tenantId}:solve:progress:log（Redis List）
- 心跳机制：每10秒写入心跳时间戳
- 超时判定：2分钟无心跳 → 标记为失败
- 状态枚举：RUNNING / SUCCESS / FAILED
```

---

## 五、REST API 设计

### 5.1 智能排产 `/smart-scheduling`

| 方法 | 路径 | 功能 |
|------|------|------|
| POST | /generate | 生成排产计划（传入策略ID等参数）|
| GET | /plan/progress | 轮询排产进度 |
| POST | /plan/pending/cancel | 放弃待确认计划 |
| POST | /plan/pending/confirm | 确认计划（pending→正式）|
| POST | /preview/plan/tasks | 预览任务列表 |
| POST | /preview/plan/resource/gantt | 预览资源甘特图 |
| POST | /preview/plan/order/gantt | 预览订单甘特图 |
| POST | /preview/plan/material/gantt | 预览物料需求甘特图 |

### 5.2 生产订单 `/production-order`

| 方法 | 路径 | 功能 |
|------|------|------|
| POST | / | 新增订单 |
| PUT | / | 修改订单 |
| PUT | /change/{id}/can/schedule | 修改是否参与排产 |
| DELETE | /{id} | 删除订单 |
| GET | /page | 分页查询 |
| GET | /{id} | 订单详情 |
| PUT | /sort | 修改订单排序 |
| GET | /supplement/list/{id} | 查询补充订单 |

### 5.3 计划策略 `/planning-strategy`

| 方法 | 路径 | 功能 |
|------|------|------|
| POST | / | 新增策略 |
| PUT | / | 修改策略 |
| PUT | /change/{id}/active | 启用/禁用 |
| DELETE | /{id} | 删除 |
| GET | /page | 分页查询 |
| GET | /{id} | 策略详情 |
| GET | /scheduling-rule/options | 获取排序规则选项列表 |

### 5.4 其他基础数据 API

- `/material`：物料管理
- `/manufacture-bom`：制造BOM管理
- `/process`：工序管理
- `/process-route`：工艺路线管理
- `/production-resource`：生产资源管理
- `/work-calendar`：工作日历管理
- `/resource-calendar`：资源-日历关系管理
- `/work-mode`：工作模式管理
- `/incoming-material-order`：来料订单管理

### 5.5 排后调整 `/task-adjust`

- 任务冻结（锁定/解锁）
- 任务合并（多任务→一个合并任务）
- 任务拆分（一个任务→多个子任务）
- 任务移动（更换资源/时间）

### 5.6 排产结果 `/schedule-result`

- 甘特图数据（资源维度、订单维度）
- 物料需求分析
- 任务列表导出

---

## 六、图算法模块

### 6.1 图数据结构

```
Graph<T>
├── nodes: List<GraphNode<T>>   // 节点集合，泛型数据负载
├── edges: List<GraphEdge>       // 有向边集合（source→target）
```

### 6.2 核心算法

#### 拓扑排序（Kahn算法）
用途：确定工艺路线的执行顺序、MRP展开顺序

```
1. 计算所有节点的入度
2. 将所有入度为0的节点入队
3. 循环出队：
   a. 取出节点，处理
   b. 将其所有后继节点入度减1
   c. 入度变为0的节点入队
4. 若已处理节点数 ≠ 总节点数 → 存在环
```

#### 环检测
用途：工艺路线保存前校验（不允许有环）

#### 多终点检测
用途：工艺路线校验（最终产出节点应唯一，即只有一个无出度节点）

---

## 七、多租户设计

### 7.1 租户隔离策略

- 数据隔离：所有业务表包含 `tenant_id` 字段，并建立索引
- 查询拦截：MyBatis-Plus 租户拦截器自动追加 tenant_id 条件
- 求解器隔离：每个租户独立的 SolverManager 实例
- 缓存隔离：Redis key 包含 tenantId 前缀

### 7.2 审计字段
所有实体继承 BasalPo，包含：
- create_by / create_by_id / create_time
- update_by / update_time

---

## 八、前端架构

### 8.1 技术选型
- Vue 3 Composition API
- TypeScript
- Vite 5 构建
- Vue Router 4 路由
- Pinia 状态管理
- Element Plus UI
- VTable 甘特图组件
- AntV G6 工艺路线图形化编辑

### 8.2 目录结构
```
src/
├── api/          # HTTP请求封装（Axios + CancelToken）
├── components/   # 公共组件
├── const/        # 常量定义
├── directives/   # 自定义指令（懒加载、滚动加载、tooltip等）
├── hooks/        # 组合式函数
├── lang/         # 国际化
├── router/       # 路由配置
├── store/        # 状态管理
├── styles/       # 全局样式（SCSS）
├── util/         # 工具函数
└── views/        # 页面视图
    └── aps/      # APS功能页面
```

### 8.3 路由设计
- 主布局 `/wel` → Layout包裹
- 动态iframe路由 `/myiframe/:routerPath`（支持嵌入外部页面）
- 404兜底处理

---

## 九、部署配置

### 9.1 应用配置

```yaml
服务端口: 31010
上下文路径: /mgr/jvs-aps
数据库: mysql://localhost:3306/jvs-aps
Redis: localhost:6379, DB=2
```

### 9.2 Docker Compose 基础设施

```yaml
MySQL:
  镜像: mysql:8.0.32
  端口: 3306
  字符集: utf8mb4
  配置: lower_case_table_names=1

Redis:
  镜像: redis:6.0
  端口: 6379
  认证: 需配置密码
```

### 9.3 前端部署
- Nginx反向代理
- Vite生产构建输出

---

## 十、排后调整子系统

### 10.1 任务冻结

```
冻结类型：
1. LOCK_SELECTED：锁定选中的任务（pinned=true）
2. LOCK_STARTED：锁定所有已开始的任务

锁定后效果：
- 该任务在下次重排时不会被移动或重新分配资源
- OptaPlanner通过@PlanningPin注解识别
```

### 10.2 任务合并

```
条件：选中的多个任务必须属于同一工序
过程：
1. 创建新的合并任务，生产数量为子任务之和
2. 子任务记录 mergeTaskCode 指向合并任务
3. 合并任务标记 mergeTask=true
4. 重新计算前后置关系
```

### 10.3 任务拆分

```
过程：
1. 将原任务拆为N个子任务
2. 各子任务数量之和 = 原任务数量
3. 子任务记录 originTaskCode 指向原任务
4. 子任务可分配到不同资源
```

### 10.4 任务移动

```
支持：
- 更换主资源
- 修改开始时间
- 系统自动级联计算影响的后续任务时间
```

### 10.5 调整数据流

```
调整操作 → 保存至 jvs_plan_task_adjust 表
                    ↓
查询时：正式任务表 UNION 调整任务表（调整优先）
                    ↓
下次重排时：正式表 + 调整表合并处理
```

---

## 十一、关键设计决策

### 11.1 为什么用双表（Pending模式）？
排产结果需用户确认后才能生效。双表设计避免了排产过程中对正式数据的影响，支持"预览→确认/放弃"的工作流。

### 11.2 为什么工艺路线存为JSON图？
工艺路线本质是DAG，用节点+边的JSON结构直接存储，避免了关系型表达DAG的复杂关联查询，同时便于前端图形化编辑器直接读写。

### 11.3 为什么用OptaPlanner而非自研调度算法？
- 成熟的元启发式搜索框架（支持模拟退火、遗传算法等）
- 增量评分计算（BAVET引擎），大幅提升求解速度
- 支持多线程并行求解
- 内置规划实体/变量/影子变量等高级抽象

### 11.4 为什么将产能设计为字符串格式？
三种产能表达方式（处理时间/处理速率/批次时间）对应不同生产场景（离散加工/连续生产/批处理），字符串格式便于灵活解析。

### 11.5 为什么用Redis追踪排产进度？
排产是异步长时间运算（最长1小时），需要：
- 前端轮询进度（Redis读取性能高）
- 心跳超时检测（防止服务意外中断后状态悬挂）
- 跨服务实例的状态共享（无状态部署）

---

## 十二、复现指南

### 12.1 核心功能复现优先级

1. **基础数据 CRUD**（物料、BOM、工序、工艺路线、资源、日历）
2. **工艺路线图形化编辑器**（DAG编辑、环检测、拓扑排序）
3. **MRP物料需求计划**（BOM展开、库存齐套、缺料分析）
4. **任务生成**（从工艺路线DAG生成任务链、建立依赖关系）
5. **约束优化求解**（接入OptaPlanner或等效求解器）
6. **异步进度追踪**（Redis心跳+轮询）
7. **双表确认机制**（Pending→正式转储）
8. **排后调整**（冻结/合并/拆分/移动）
9. **可视化报告**（资源甘特图、订单甘特图、物料需求图）

### 12.2 技术选型可替代方案

| 原技术 | 可替代 |
|--------|--------|
| OptaPlanner | OR-Tools, Timefold, 自研GA/SA |
| MyBatis-Plus | JPA/Hibernate, Prisma, SQLAlchemy |
| Spring Boot | 任何Web框架（FastAPI, Gin, Express等）|
| Redis | 任何支持TTL的KV存储 |
| Vue 3 | React, Angular, Svelte |
| MySQL | PostgreSQL（推荐）, 任何RDBMS |

### 12.3 最小可行实现清单

```
□ 数据库表创建（13张核心表）
□ 物料/BOM/工序/资源/日历 CRUD
□ 工艺路线 DAG 存储与校验
□ 生产订单管理
□ 排产策略配置
□ MRP 齐套计算
□ 任务自动生成
□ 约束求解器接入
□ 排产进度追踪
□ 结果持久化（双表模式）
□ 甘特图可视化
□ 排后任务调整
```

---

## 十三、数据流示意

### 13.1 排产生成数据流

```
[生产订单] + [策略] → PlanningSmartService.generate()
                           │
                           ├→ 加载 BasicData{物料, BOM, 资源, 工艺路线, 日历, 订单}
                           │
                           ├→ SolveService.solve() [异步]
                           │       │
                           │       ├→ MRP齐套计算 → 缺料图
                           │       │
                           │       ├→ 任务生成 → ProductionTask列表
                           │       │
                           │       ├→ 构造SchedulingSolution
                           │       │
                           │       ├→ OptaPlanner.solve() → 最优Solution
                           │       │
                           │       ├→ 校验合规性
                           │       │
                           │       └→ 保存至Pending表
                           │
                           └→ Redis写入进度状态

[前端轮询进度] ← Redis读取
[前端预览甘特图] ← 查询Pending表
[用户确认] → Pending表 → 正式表 + 更新订单状态
```

### 13.2 排后调整数据流

```
[用户操作] → TaskAdjustController
                │
                ├→ freeze: 更新pinned字段
                ├→ merge: 创建合并任务 + 修改子任务关系
                ├→ split: 创建子任务 + 分配数量
                └→ move: 修改资源/时间
                         │
                         ↓
              保存至 jvs_plan_task_adjust 表
                         │
                         ↓
              查询时：正式表 ∪ 调整表（调整优先覆盖）
```

---

## 十四、扩展点

| 扩展方向 | 说明 |
|---------|------|
| 多工厂协调 | 增加工厂维度，资源按工厂分组 |
| 外购件约束 | 扩展MRP处理PURCHASED类物料的采购计划生成 |
| 报工反馈闭环 | 已有work_report表基础，扩展自动更新任务完成进度 |
| 模拟仿真 | 基于现有排产引擎，扩展"What-If"场景模拟 |
| AI预测 | 接入历史数据训练需求预测模型 |
| WebSocket推送 | 替代轮询，实时推送排产进度 |
| 权限控制 | 基于角色的操作权限（当前未实现细粒度权限）|

---

*报告生成时间：2026-05-25*
